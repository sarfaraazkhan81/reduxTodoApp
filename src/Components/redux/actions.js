export const Items = (item) => ({
  type: "ITEMS",
  payload: item,
});

export const UUID = (item) => ({
  type: "UUID",
  payload: item,
});
export const EditItem = (item) => ({
  type: "EDIT_ITEM",
  payload: item,
});
export const ITEM = (item) => ({
  type: "ITEM",
  payload: item,
});
